/** Default wait timeout in seconds — 2h fallback if no agent timeout configured. */
export const DEFAULT_WAIT_TIMEOUT = 7200;

/** Seconds between process alive checks. */
export const CHECK_INTERVAL = 30;

/** Sandbox directory for user-defined logs and job artifacts. */
export const USER_DEFINED_LOGS = '/data/logs/user-defined';
