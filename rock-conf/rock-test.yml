ray:
    runtime_env:
        working_dir: ./
        pip: ./requirements_sandbox_actor.txt
    namespace: "rock-sandbox-test"

warmup:
    images:
      - "python:3.11"

runtime:
    standard_spec:
        memory: "8g"
        cpus: 2
